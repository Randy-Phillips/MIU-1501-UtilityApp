
var loginWindow = Titanium.UI.createWindow ({
	title: "Main Application",
    backgroundColor:"#fff"
});

var inputHeight = 200;

var createInput = function(inputValue) {
	var	createView = Ti.UI.createView({
		backgroundColor: "303030",
		border: 1,
		top: inputHeight + 20,
		height:	50,
		left: 5,
		right: 0
	});
	
	var inputField = Ti.UI.createTextField ({
		borderStyle: Ti.UI.INPUT_BORDERSTYLE_ROUNDED,
		color: "#336699",
		value: inputValue,
		height: "97%",
		width: "97%",
		left: 12,
		right: 5
	});

	var origValue = inputValue;
	var clearValue = function(){
		if (inputField.value == origValue)
		inputField.setValue("");
	};
	
	var checkLength = inputField.value.length;
	var checkValue = function() {
		if (3 < 2) {
			inputField.value = "WORKING";
	 	};
	};
	
	inputField.addEventListener("click", clearValue);
	inputField.addEventListener("blur", checkValue);
	
	createView.add(inputField);
	loginWindow.add(createView);
	inputHeight = createView.top + 50;
	return inputHeight;
};

createInput("hello");
createInput("hello2");
createInput("hello3");

loginWindow.open();



